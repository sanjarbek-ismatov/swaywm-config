```bash 
sudo dnf install power-profiles-daemon nvtop radeontop powertop htop btop fira-code-fonts git gh lxappearance qt5ct qt6ct kvantum timeshift vlc neofetch fastfetch gammastep gammastep-indicator swappy nautilus copyq 
```

```bash
echo -n > "$HOME/.config/sway/config.d/90-bar.conf"
```